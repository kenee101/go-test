services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADDR=:8080
      - MONGO_URI=${MONGO_URI}
      - MONGO_DB=${MONGO_DB}
      - JWT_SECRET=${JWT_SECRET}
    restart: unless-stopped
